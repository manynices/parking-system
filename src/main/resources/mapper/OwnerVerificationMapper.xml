<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.experiment.parkingsystem.mapper.OwnerVerificationMapper">

    <!-- 原有 insert 和 selectLatestByUserId 保持不变 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="applicationId">
        INSERT INTO owner_verification_application
            (user_id, name, id_card, address, proof_images, status, create_time)
        VALUES
            (#{userId}, #{name}, #{idCard}, #{address}, #{proofImages}, #{status}, #{createTime})
    </insert>

    <select id="selectLatestByUserId" resultType="com.experiment.parkingsystem.entity.OwnerVerification">
        SELECT * FROM owner_verification_application
        WHERE user_id = #{userId}
        ORDER BY create_time DESC LIMIT 1
    </select>

    <!-- 【新增】 -->
    <update id="updateStatus">
        UPDATE owner_verification_application
        SET status = #{status},
            verifier_name = #{verifierName},
            remark = #{remark},
            verified_time = #{verifiedTime}
        WHERE application_id = #{applicationId}
    </update>

    <select id="selectById" resultType="com.experiment.parkingsystem.entity.OwnerVerification">
        SELECT * FROM owner_verification_application WHERE application_id = #{applicationId}
    </select>

    <select id="selectPendingList" resultType="com.experiment.parkingsystem.entity.OwnerVerification">
        SELECT * FROM owner_verification_application
        WHERE status = '待审核'
        ORDER BY create_time ASC
    </select>

</mapper>